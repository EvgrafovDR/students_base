
<div class="admhead">
  <h1>Факультеты</h1>
</div>
<% if current_user.admin? %>
<%= link_to "Добавить", new_faculty_path, class: "btn btn-default btn-lg", role: "button" %>
<% end %>
<table id="myTable" class="table table-hover">
  <thead>
  <tr>
      <th>ID</th>
      <th>Название</th>
      <th></th>
      <th></th>
  </tr>
  </thead>

    <% @faculties.each do |faculty| %>
        <%= render partial: 'faculties/faculty', locals: {faculty: faculty} %>
    <% end %>

  </table>